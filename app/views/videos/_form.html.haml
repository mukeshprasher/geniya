= simple_form_for(@video) do |f|
  = f.error_notification

  .form-inputs
    = f.hidden_field :album_id, value: album.id
    = f.input :name
    
    = f.file_field :file_attachment
    %br
    %br
    %p
      = f.button :submit, value: "Add Video"
