%select#location_state_id.select.required{name: "location[state_id]"}
  %option{value: "0"}   Select State
  - c.each do|s|
    %option{value: "#{s.id}"}   #{s.name}
    = City.find_all_by_state_id("#{s.id}")





