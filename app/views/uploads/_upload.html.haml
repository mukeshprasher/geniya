.content1{:id => "#{upload.id}"}
  =image_tag upload.file_attachment.url, alt: upload.file_attachment.url(:original)
  = link_to "×", comment_path(comment), :method => :delete, :remote => true,  :disable_with => "×", :class => 'comment-delete' , :id => "comment-delete-#{update.id}"

  .album-content
    %p
      = upload.name
    %p
      = upload.title
    %p 
      = upload.description
    = link_to 'Edit', edit_upload_path(upload)
