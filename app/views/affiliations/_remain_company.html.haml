%p.aff_org_name{ :id => "current_company-#{@a.id}" }
  %a{href:"/organizations/"+"#{@a.organization.id}"}
    = @a.organization.name
  = link_to "×", affiliation_path(@a.id), :method => :delete, :remote => true,  :disable_with => "×", :class => 'affiliation-delete' , :id => "affiliation-delete-#{@a.id}"
  
  

