.innerBenner
  .wrapper
    .news_feeds
      %h2 
        Message Sent to Gmail Contacts
.wrapper
  .row-fluid.spaced-top
    .span2.spaced-left
      .btn-group.btn-group-vertical.btn-block
        = link_to 'My Profile', current_user, class: 'btn'
.detail_form_container
  - if !@contacts.nil?
    - @contacts.each do |c|
      %ul
        %li
          = c[:email]   
