.innerBenner
  .wrapper
    .news_feeds
      %h2
        - if !@contacts.nil? 
          Invitation message has been sent.
        - else
          You have not contact
.wrapper
  .row-fluid.spaced-top
    .span2.spaced-left
      .btn-group.btn-group-vertical.btn-block
        = link_to 'My Profile', current_user, class: 'btn'
.detail_form_container
  - if !@contacts.nil?
    - @contacts.each do |c|
      %ul.invite_frnd_list
        %li
          = c[:email]   
