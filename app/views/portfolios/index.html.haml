= render "portfolio_menu"
.inneContainer
  .wrapper
    .box_section
      %ul#portfolios
        - @albums.each do |album|
          = render 'shared/album_view', album: album
      #infinite-scrolling
        = will_paginate @albums      
      .clear

