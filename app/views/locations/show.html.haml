.wrapper
  .news_feeds
      
.wrapper
  .row-fluid.spaced-top
    .span2.spaced-left
      .btn-group.btn-group-vertical.btn-block
        = link_to 'My Profile', current_user, class: 'btn'
        = link_to 'Back', locations_path, class: 'btn'
.detail_form_container
  %h3
    City:
    = @location.city.name.capitalize
  %h4
    Pin/zip Code:
    = @location.pin.code
  %h4
    Street Address:
    = @location.street_address.capitalize

